struct Source
{
    let Text: string
}

enum Lisp<T>
{
    case Atom(T)
    case Cons(Lisp<T>)
    case Nil()
}

enum Token
{
    case EOF, EORROR
    case Identifier(id: string)
    case Number(num: string)
}
